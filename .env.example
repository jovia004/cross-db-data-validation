# Keep this file in the project root (same folder as run.py).
# Copy to .env and set the values below. Do not commit .env to git.

# Primary database (SQL Server)
PRIMARY_DB_HOST=
PRIMARY_DB_PORT=1433
PRIMARY_DB_NAME=
PRIMARY_DB_USER=
PRIMARY_DB_PASSWORD=

# Shadow database (PostgreSQL)
SHADOW_DB_HOST=
SHADOW_DB_PORT=5432
SHADOW_DB_NAME=
SHADOW_DB_USER=
SHADOW_DB_PASSWORD=
